version: "3.8"

services:
  claude:
    build: .
    container_name: claude-code
    volumes:
      - .:/workspace
      - ~/.claude:/root/.claude
      - ~/.claude.json:/root/.claude.json
    working_dir: /workspace
    stdin_open: true
    tty: true
